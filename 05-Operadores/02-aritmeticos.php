<?php
    // $a = 2;
    // $b = 5;

    // $c = $a + $b;
    // $c = $a - $b;
    // $c = $a * $b;
    // $c = $a / $b;
    // $c = $b % $a; // Operador Módulo

    // echo $c;

    // Ley de signos
    // echo 4 * 5 - 6 + 4 * 2 + 1 * 0;

    // Prioridad con parentésis
    echo 4 * (5 - 6) + 4 * 2 + 1 * 0;
?>